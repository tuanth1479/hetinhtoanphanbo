
{
  "name": "hello-jenkins",
  "description": "hello jenkins",
  "version": "1.0.0",
  "private": true,

  "app": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "express": "^3.12.0"
  },

  "devDependencies": {
    "mocha": "^3.0.1",
    "supertest": "6.3.3"
  },
  "test": "mocha --exit",
  "keywords": [
    "node",
    "docker",
    "dockerfile"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/tuanth1479/hetinhtoanphanbo"
  }
}
